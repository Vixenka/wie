/*pub struct TransportHost {}

impl TransportHost {
    pub fn bind(port: u32) -> Result<TransportHost, ()> {
        tracing::info!("[Host] Setting up listening socket on port {}", port);

        Ok(Self {})
    }
}*/
